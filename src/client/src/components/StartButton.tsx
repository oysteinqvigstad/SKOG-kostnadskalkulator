import {Button} from "react-bootstrap";

export function StartButton() {
    return (
        <Button variant="primary" size="lg" href="/kalkulator">
            Start kalkulatoren
        </Button>
    )
}