import { GraphDisplayNode } from "@skogkalk/common/dist/src/parseTree/nodes/displayNode";

describe('GraphDisplayNode', ()=>{
    it('should do something', () => {
        expect(1).toEqual(1);
    })
})