{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install:client": "npm --prefix client install",
    "install:api": "npm --prefix api install",
    "install": "npm run install:client && npm run install:api",
    "ci:client": "npm --prefix client ci",
    "ci:api": "npm --prefix api ci",
    "ci": "npm run ci:client && npm run ci:api",
    "build:client": "npm --prefix client run build",
    "build:api": "npm --prefix api run build",
    "build": "npm run build:client && npm run build:api",
    "start": "npm --prefix api start",
    "test:client": "npm --prefix client test",
    "test:api": "npm --prefix api test",
    "test": "npm run test:client && npm run test:api",
    "dev:client": "npm --prefix client start",
    "dev:api": "npm --prefix api run dev",
    "dev": "npm run dev:client && npm run dev:api"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
